let reportAndThrowError,stripLeadingSlash,stripTrailingSlash,fetchLogo,fetchPkg,fetchStats;_ae0‍.x([["collectiveSlugFromUrl",()=>collectiveSlugFromUrl],["collectiveUrl",()=>collectiveUrl],["collectiveLogoUrl",()=>collectiveLogoUrl],["collectiveDonationText",()=>collectiveDonationText],["getCollective",()=>getCollective],["collectiveDonationUrl",()=>collectiveDonationUrl],["retrieveDonationSlug",()=>retrieveDonationSlug]]);_ae0‍.w("./misc",[["reportAndThrowError",["reportAndThrowError"],function(v){reportAndThrowError=v}],["stripLeadingSlash",["stripLeadingSlash"],function(v){stripLeadingSlash=v}],["stripTrailingSlash",["stripTrailingSlash"],function(v){stripTrailingSlash=v}]]);_ae0‍.w("./fetch",[["fetchLogo",["fetchLogo"],function(v){fetchLogo=v}],["fetchPkg",["fetchPkg"],function(v){fetchPkg=v}],["fetchStats",["fetchStats"],function(v){fetchStats=v}]]);


       const collectiveSlugFromUrl = url => url.substr(url.lastIndexOf('/') + 1).toLowerCase().replace(/\.json/g, '');_ae0‍.j(["collectiveSlugFromUrl"]);

       const collectiveUrl = pkg => {
  const url = pkg.collective && pkg.collective.url

  if (!url) {
    _ae0‍.a("reportAndThrowError",reportAndThrowError)('No collective URL set!')
  }

  return _ae0‍.a("stripTrailingSlash",stripTrailingSlash)(url)
};_ae0‍.j(["collectiveUrl"]);

// use pkg.collective.logo for "legacy"/compatibility reasons
       const collectiveLogoUrl = pkg => pkg.collective.logo || pkg.collective.logoUrl || false;_ae0‍.j(["collectiveLogoUrl"]);

       const collectiveDonationText = pkg => (pkg.collective.donation && pkg.collective.donation.text) || 'Donate:';_ae0‍.j(["collectiveDonationText"]);

       const getCollective = async pkgPath => {
  const pkg = _ae0‍.a("fetchPkg",fetchPkg)(pkgPath)
  const url = collectiveUrl(pkg)
  const baseCollective = {
    url,
    slug: collectiveSlugFromUrl(url),
    logoUrl: collectiveLogoUrl(pkg),
    donationUrl: collectiveDonationUrl(pkg),
    donationText: collectiveDonationText(pkg)
  }
  const logoUrl = baseCollective.logoUrl
  const promises = [_ae0‍.a("fetchStats",fetchStats)(url)].concat(logoUrl ? _ae0‍.a("fetchLogo",fetchLogo)(logoUrl) : [])

  const [stats, logo] = await Promise.all(promises)

  return Object.assign(baseCollective, { stats, logo })
};_ae0‍.j(["getCollective"]);

       const collectiveDonationUrl = pkg => {
  const defaultDonationAmount = pkg.collective.donation && pkg.collective.donation.amount

  let donateUrl = `${collectiveUrl(pkg)}/${retrieveDonationSlug(pkg)}`

  if (defaultDonationAmount) {
    return `${donateUrl}/${defaultDonationAmount}`
  }

  return donateUrl
};_ae0‍.j(["collectiveDonationUrl"]);

       const retrieveDonationSlug = pkg => {
  const rawDonationSlug = (pkg.collective.donation && pkg.collective.donation.slug)

  if (!rawDonationSlug) {
    return 'donate'
  }

  return _ae0‍.a("stripLeadingSlash",stripLeadingSlash)(rawDonationSlug)
};_ae0‍.j(["retrieveDonationSlug"]);
